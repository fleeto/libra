# Static-first completion for implemented Libra capabilities (2026-09-23)

> **Template:** [plan-template.md](plan-template.md), v2.8. **Status:** revised draft, not implementation-approved. All implementation cards are pending; no review PASS, test result, release, or backend inspection is claimed.
>
> **Authority:** the user's current requirements supersede the previous dynamic-first draft: cover implemented Libra capabilities, including Libra-only capabilities; complete and accept static completion before starting dynamic completion. Git is a behavioral reference, not the scope authority.
>
> **Tracking obligation:** update [plan-status.md](plan-status.md) in the same change as every card transition, including acceptance, date, commit/version, dependencies and deferred items. Update the date-plan index in [plan-long.md](plan-long.md). No LR implementation status is changed by this plan.

## Document responsibility

Deliver complete, maintainable completion coverage for the public CLI that Libra actually implements. This document contains both the implementation plan and its executable task cards; the coverage ledger defined below is the per-parameter acceptance artifact.

### Scope

Every public command, subcommand, public alias, flag and value-taking argument in the compiled command tree, including Libra-only and feature-gated capabilities. Static coverage includes syntax, fixed choices and appropriate filesystem hints. Dynamic coverage adds existing repository/configuration objects where the command already accepts them.

### Non-goals

Do not implement missing Git behavior, new business commands, remote-branch guessing, new refspec semantics, schema migrations, credential discovery, or network-backed suggestions. Do not execute user hooks, automation actions, external agent binaries or shell configuration edits. Hidden internal commands receive an explicit exclusion record. Free text, secrets and newly created identifiers are classified deliberately, not populated with misleading existing-object candidates.

### Success definition

1. Every current public command and argument has an explicit coverage disposition and owner.
2. Static completion is independently accepted and released before any dynamic implementation begins.
3. Dynamic mode is opt-in and preserves the accepted static mode.
4. Candidates match the consuming command's existing value semantics, identity scope and context.
5. Both stages have real shell evidence, documentation, release coverage and reconciled tracking.

## Fact baseline

Baseline checked on 2026-09-23; refresh file anchors, HEAD and feature surfaces under ER-02 before execution.

| Surface | Verified fact | Evidence |
|---|---|---|
| Checkout | Git metadata exists; `.libra` does not. Root workspace notes claiming the inverse are stale for this checkout. | Read-only `git status`; observed branch `completion-completion`, HEAD `ad70397b6`; revalidate the authorized branch at execution |
| Package | Version 0.23.46; clap 4.6.1; clap_complete 4.6.6 without dynamic feature | `Cargo.toml:3`, `:37-38` |
| Generator | AOT `clap_complete::generate`; binary name hard-coded to libra | `src/command/completions.rs:48-85` |
| Existing compatibility | Five shells: bash, zsh, fish, powershell, elvish; JSON data contains shell/script; invalid or absent shell exits 129 | `tests/command/completions_test.rs` |
| Command authority | The compiled clap tree and handlers, not command-group prose or Git's command list | `src/cli.rs`; `src/command/` |
| Session | show, stop, resume, derive-tool-calls consume session IDs; list has agent/state filters | `src/command/agent/session.rs:23-89` |
| Checkpoint | show, export, rewind; list has --session | `src/command/agent/mod.rs:280-357` |
| Agent names | enable/disable use --agent; add/remove use positional names; these are String collections | `src/command/agent/mod.rs:208-227` |
| Automation | run --rule takes a name; history has no rule argument; rule definitions load from configuration | `src/command/automation.rs:54-76`, `:215-221` |
| Other local domains | worktree paths, workspace IDs, operation refs, layer names, revision-valued metadata/deps parameters already exist | `src/command/{worktree,op,layer,metadata,deps}.rs` |
| Configuration | Global first-use migration can create directories/locks/copies | `src/internal/config/global_migration.rs:67`; plan-20260919 |
| Prior delivery | Initial static generator is already shipped; this is coverage expansion, not a new completions command | `docs/development/gap/lore.md`; `docs/development/commands/completions.md` |

### Current gaps

| ID | Gap | Impact | Owner |
|---|---|---|---|
| GAP-CP-01 | No exhaustive current CLI/argument coverage evidence | Public and Libra-only surfaces can be missed | CP-00, CP-01..03 |
| GAP-CP-02 | Static alias installation and shell execution coverage are incomplete | Existing functionality is hard to discover/use | CP-04..06 |
| GAP-CP-03 | Runtime object candidates are absent | Users must remember/copy identifiers | CP-07..19 |
| GAP-CP-04 | Previous draft used nonexistent command paths and an incorrect automation data source | Unimplementable registration and misleading tests | CP-00, CP-12..17 |
| GAP-CP-05 | Scope, no-write and cross-plan contracts were not explicit enough | Wrong-scope candidates or completion side effects | CP-07 and every provider card |

## Coverage ledger and scope contract

CP-00 produces `docs/development/plan/plan-20260923-coverage.tsv`. It is a reviewed inventory, not a second hand-maintained command parser. Columns:

`command_path`, `arg_id`, `feature_profile`, `source_anchor`, `accepted_semantics`, `static_disposition`, `dynamic_disposition`, `candidate_source`, `context_and_scope`, `owner`, `test_case`, `status`, `reason`.

Allowed dispositions: static `grammar`, `fixed-values`, `file`, `directory`, `free-text`, `secret`, `new-name`, `hidden`; dynamic `existing-object`, `reuse-provider`, `not-applicable`, `external-deferred`, `unassigned`. Every public node and argument must appear, including valueless flags; hidden entries carry a reason. `unassigned` is an audit failure, never a completion-state exemption. A missing underlying capability is `not-applicable` with handler evidence, not a task to implement that capability.

The inventory must cover default and supported feature-dependent CLI trees (including fastcdc media and platform/worktree-fuse surfaces). Do not enable live services merely to enumerate commands. Public aliases must be accepted as invocation paths; whether a non-visible alias is advertised follows current clap visibility. Annotation changes must not accidentally narrow accepted command input.

| Coverage family | Mandatory survey | Initial dynamic owner |
|---|---|---|
| Setup/configuration | init, clone, config, completions, accepted global arguments | CP-09 or CP-19 for eligible local names/keys |
| Working tree | add/rm/mv/restore/clean/status, paths, worktree, stash, sparse-view, hydrate, lfs/media | CP-10/11/19; filesystem hints in CP-03 |
| History/refs | all implemented revision consumers, including plumbing and Libra revision command | CP-08; index-aware paths in CP-19 |
| Remote/cloud | remote/fetch/pull/push, cloud/cache/auth/mega2 and other shipped commands | CP-09/19; no network or credential suggestions |
| Captured agents | names, sessions, checkpoints, workspaces, graph and public filters | CP-12..15; agent push --remote uses CP-09 |
| Automation/local extensions | automation, op, layer, metadata, deps, alternates, review/investigate/sandbox/service | CP-16..19; unsuitable free text is explicitly classified |

Existing-object arguments and creation destinations are different. Support local remote-tracking refs, special refs, revision expressions or refspec components only where the current consumer accepts them. CP-00 records that evidence per parameter. Git documentation can inform UX but cannot add acceptance semantics. Stash and basic worktree completion belong to shared Git-like workflows; agent workspace/doctor behavior remains Libra-specific.

Any uncovered eligible existing-object domain discovered by CP-00 becomes a bounded named card before implementation approval. CP-19 checks for omissions; it is not an unlimited implementation bucket. No fixed estimate of card count can override complete coverage.

## Relationships with other plans

| Plan | Relationship | Required handling |
|---|---|---|
| plan-20260918 / 20260904 / 20260912 / 20260916 / issues/483 | Existing DEP-CLI-mirror serializes src/cli.rs owners | DEP-CP-02: join the existing file-owner queue for affected cards; no blanket dependency on whole-plan completion |
| plan-20260919 | Configuration path and first-use migration contract | DEP-CP-03: consume read-only path resolution; never run migration while completing |
| plan-20260921 | REL-VG-01 reserves Cargo/installers/compatibility and related files | DEP-CP-04: honor the whole declared window, not merely simultaneous version bumps |
| issues/476, 478, 480 | Worktree, revision and remote command evolution | DEP-CP-05: freeze implemented parameter contracts at card start; serialize actual file intersections |
| plan-20260819 | Agent storage/identity evolution | DEP-CP-06: identify consumed models and read APIs before CP-13..15; scope filters must follow current handlers |
| plan-20260920 | Removed Code/Publish surfaces | Do not revive retired commands or their completion entries |
| plan-long | Cross-cutting CLI usability, no new LR commitment | Add date-plan index; do not change unrelated roadmap statuses |

Later feature cards that add/change public parameters must update completion metadata, the coverage ledger and regression evidence in the same change. This plan does not block unrelated feature development.

## Review conclusions and revision history

| Dimension | Draft assessment | Required follow-up |
|---|---|---|
| Scope/reasonableness | Implemented-capability authority; equal coverage for Libra-only commands | CP-00 exhaustive inventory |
| Feasibility | Existing AOT generator retained; dynamic API integration gated after static release | Verify pinned upstream API at CP-07 |
| Granularity | One delivered behavior per implementation card; shared infrastructure carries a working consumer | Recount after inventory expansion |
| Dependencies | Conservative serial chain avoids shared registry/test/doc collisions | Register external file reservations |
| Correctness/compatibility | Static default retained; no parser-semantic expansion | Five-shell and input-compatibility evidence |
| Security/data flow | Read-only, identity-scoped, bounded local candidate retrieval | Fault and no-write tests |
| Performance/reliability | End-to-end budgets include process startup, discovery and lock waits | CP-07/20 evidence |
| Maintainability | Compiled command tree authoritative; supplementary metadata validated against it | Missing/stale-row guard |
| Testing/docs/recovery | Each card owns tests/docs and releases; reverse dependency recovery | External docs access and shell evidence remain pending |

| Date | Trigger | Change | Old → new | Affected references |
|---|---|---|---|---|
| 2026-09-23 | Initial draft | Dynamic-first proposal, not executed | Original CP-01..05 | Historical draft superseded |
| 2026-09-23 | User scope/order corrections | English rewrite; static first; all implemented Libra capabilities; retain static mode | Old CP-01 → CP-07; CP-02 → CP-08/09/19; CP-03 → CP-04; CP-04 → CP-05; CP-05 → CP-10..18; new inventory/static/acceptance cards | Entire DAG, matrix, milestones, tracking and acceptance |

## Accepted design decisions

### ADR-CP-01: Capability-led scope

- **Status:** Accepted (user direction, 2026-09-23).
- **Context:** Copying Git ignores Libra's implemented contracts and unique objects.
- **Decision:** Inventory actual public CLI behavior and cover all suitable parameters.
- **Alternatives considered:** Git parity checklist as scope authority; rejected.
- **Consequences:** Unsupported business behavior stays outside this plan; undiscovered domains must be assigned before implementation.
- **Revisit when:** Current commands or supported features change.

### ADR-CP-02: Static acceptance precedes dynamic implementation

- **Status:** Accepted (user direction, 2026-09-23).
- **Context:** The static baseline is not yet exhaustively verified.
- **Decision:** CP-06 is a hard barrier before CP-07; keep default AOT output. Add explicit --dynamic only in stage two, retaining the existing shell/script JSON data contract.
- **Alternatives considered:** Immediately replace static output; rejected.
- **Consequences:** Both modes remain tested; dynamic is optional, not a prerequisite for static installation.
- **Revisit when:** A separately approved compatibility decision changes the default.

### ADR-CP-03: Metadata and providers supplement one command tree

- **Status:** Proposed implementation design, subject to plan review.
- **Context:** Scattered candidate rules drift; one giant registry card is not independently reviewable.
- **Decision:** Use src/internal/completion/{mod.rs,static_metadata.rs,registry.rs,context.rs,providers/}; supplement the real clap tree without changing command parsing. Fixed choices must already be enforced/recognized by handlers. Validate every path/arg ID against the compiled tree.
- **Alternatives considered:** Reconstructing commands in a parallel parser; rejected. If the pinned API cannot attach required metadata without changing validation, use bounded derive annotations with revised write sets/cards before implementation.
- **Consequences:** No silent fallback to edits across 15+ command files; G-03/G-04/G-10 are rerun first.
- **Revisit when:** The pinned clap API cannot preserve the input contract.

### ADR-CP-04: Bounded read-only dynamic requests

- **Status:** Proposed implementation design, subject to plan review.
- **Context:** TAB must not execute normal command preflight or migrations.
- **Decision:** Intercept only recognized completion requests before side-effectful startup; preserve normal invocation otherwise. Local sources only. Expected unavailable data yields empty candidates with exit 0 and clean stderr; diagnostics are opt-in, sanitized and outside the completion stream.
- **Alternatives considered:** Calling list/mutation CLI handlers; rejected because they may bootstrap, migrate or produce unrelated output.
- **Consequences:** Repository, GlobalConfig and SystemConfig are read-only roles; Derived has no persistent writer. Migration/bootstrap/top-up allowlists are empty for every role. Unknown/new schemas return no candidates. Existing path-resolution semantics are reused without migration.
- **Revisit when:** A future explicit product requirement needs remote suggestions.

## Global engineering constraints

Inherit all template GC-01 through GC-13 without waiver. GC-CP-01: no network, hooks, external agent execution, vault unlock, schema mutation or persistent cache during a completion request. GC-CP-02: never write shell startup files. GC-CP-03: the accepted static baseline is a regression contract for every dynamic card.

## Mandatory execution requirements

Inherit ER-01 through ER-14 and G-01 through G-11. User authorization and filesystem boundaries still apply; this document does not authorize backend access, installation, commits, pushes or releases by itself. Detect actual VCS before status/signing operations. Record the authorized implementation branch at CP-00 and refuse accidental branch drift.

All pushed cards require fmt and clippy. Release integration evidence uses nextest. The full-suite command is:

```sh
source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2
```

Missing environment files are an explicit blocker; do not fabricate secrets, run against production, or substitute cargo test --all as acceptance evidence. The pinned nextest version follows the current workflow. New targets update Cargo.toml, tests/INDEX.md and (for compat targets) tests/compat/README.md. Environment/CWD mutations use project isolation helpers and keyed serial lanes/registry regeneration.

## Implementation order

CP-00 inventory → CP-01 grammar → CP-02 fixed choices → CP-03 path hints → CP-04 static alias → CP-05 installation → CP-06 STATIC ACCEPTANCE → CP-07 dynamic opt-in → CP-08 refs → CP-09 remotes → CP-10 worktrees → CP-11 stash → CP-12 agent names → CP-13 sessions → CP-14 checkpoints → CP-15 workspaces → CP-16 automation → CP-17 operations → CP-18 layers → CP-19 coverage reconciliation → CP-20 FINAL ACCEPTANCE.

The order is intentionally serial because registry, tests, ledger and docs intersect. Semantic prerequisites remain documented per card. New domain cards discovered by CP-00 are inserted before CP-19 with explicit predecessor/successor edges. Do not start CP-07 until CP-06 is done/complete, including applicable remote gates.

### Dependency register

| ID | Direction/type | Owner and object | Availability evidence | Failure policy |
|---|---|---|---|---|
| DEP-CP-01 | incoming/external docs | Executor; ../libra-backend website | Explicit access authorization, VCS identity, verified cf branch, clean target files, exact page paths | Block applicable docs/release acceptance; never browse sibling files without authorization |
| DEP-CP-02 | incoming/file exclusion | Existing DEP-CLI-mirror owners | No conflicting active src/cli.rs writer; record reservation in plan-status | Queue affected card only |
| DEP-CP-03 | incoming/read contract | plan-20260919 config owners | Read-only path/API contract and legacy-only fixtures | Block CP-07/09/12 as applicable; no auto-migration shortcut |
| DEP-CP-04 | incoming/window exclusion | plan-20260921 REL-VG-01 | Window is inactive or explicit sequencing permits access to intersecting files | No writes during reserved window |
| DEP-CP-05 | incoming/contract and file exclusion | issues/476/478/480 owners | Refreshed handler/arg anchors and no intersecting active writer | Rebase inventory or queue relevant card |
| DEP-CP-06 | incoming/agent read contract | plan-20260819 owners | Current identity-scoped read APIs identified and stable for tested tree | Block affected provider, not static stage |
| DEP-CP-07 | incoming/validation environment | Executor | Required nextest/env files and five shell runners available in isolated environments | Record unavailable shell separately; no all-shell PASS without evidence |

### Release groups and concurrency windows

No parallel implementation in this plan. Independent implementation/release cards execute their own C/D gates, patch increment and signed/DCO commit when authorized. Version surfaces are whatever compat_version_surface_sync currently declares, plus toolchain-generated Cargo.lock and artifacts. Never hand-edit Cargo.lock. One publisher owns the release window; no other card may modify its R set. CP-00 inherits CP-06 coverage; CP-19 inherits CP-20 coverage. Those audit cards are no-release and do not independently push or bump.

### Phases and milestones

| Phase | Entry | Exit |
|---|---|---|
| 0: inventory | Draft review permits CP-00 audit | Exhaustive ledger and any needed extra cards are reviewed; applicable implementation reviews PASS |
| 1: static | CP-00 audit handoff is accepted | CP-06 done/complete; every static row accepted |
| 2: dynamic | CP-06 done/complete | CP-20 done/complete; every eligible dynamic row accepted |

## Task cards

### Field defaults and exceptions

Task type implementation; lifecycle pending; acceptance empty; scope M; release independent; version increment patch; rollback revert; C/D coverage self. Migration and rollback: no schema changes; revert in reverse dependency order, while published artifacts are corrected by a forward release. Security/privacy: GC-07/11/13 and GC-CP-01..03. Static generation performs no repository/config access. Dynamic request budget: 500 total candidates after literal-prefix filtering and deterministic ordering; 100 ms provider deadline and 250 ms process-level deadline measured on declared fixtures/hardware, including locks/discovery. Deadline expiry returns empty candidates, not a hang. If infeasible, revise the budget with evidence before acceptance.

Every card includes its matching coverage-ledger rows and this plan/status/index tracking. Standard completion docs bundle = docs/commands/completions.md, docs/commands/zh-CN/completions.md, docs/development/commands/completions.md, COMPATIBILITY.md and the authorized backend completions page. Provider cards additionally update both language pages for consuming commands assigned in the frozen ledger and corresponding backend pages. CP-00 enumerates those exact paths before any implementation; these are bounded accompanying docs, never permission to edit arbitrary commands. Backend page paths remain an external prerequisite, not a claim that they exist.

Global strong gates are not counted as card-specific Verification predicates. Acceptance counts below are individual acceptance predicates, not folded lists. No EX waiver is requested. Static metadata cards must remain within four behavior landings/twelve production files; inventory-driven expansion beyond that requires splitting and re-review, not an oversized fallback.

### Granularity audit

| Card | Type | AC | VER | Behavior landings / production files | Scope | Predecessor | Release | Exception |
|---|---|---|---|---|---|---|---|---|
| CP-00 | audit | 5/20 | 2/20 | 0/0 | S | none | no-release | none |
| CP-01 | implementation | 6/8 | 2/8 | 2/5 | M | CP-00 | independent | none |
| CP-02 | implementation | 4/8 | 2/8 | 1/1 | M | CP-01 | independent | none |
| CP-03 | implementation | 5/8 | 2/8 | 1/1 | M | CP-02 | independent | none |
| CP-04 | implementation | 5/8 | 2/8 | 1/1 | M | CP-03 | independent | none |
| CP-05 | implementation | 4/8 | 2/8 | 2/2 | M | CP-04 | independent | none |
| CP-06 | release | 4/12 | 2/12 | 0/0 | M | CP-05 | independent | none |
| CP-07 | implementation | 8/8 | 2/8 | 4/8 | M | CP-06 | independent | none |
| CP-08 | implementation | 5/8 | 2/8 | 2/2 | M | CP-07 | independent | none |
| CP-09 | implementation | 5/8 | 2/8 | 2/2 | M | CP-08 | independent | none |
| CP-10 | implementation | 4/8 | 2/8 | 2/2 | M | CP-09 | independent | none |
| CP-11 | implementation | 3/8 | 2/8 | 2/2 | M | CP-10 | independent | none |
| CP-12 | implementation | 4/8 | 2/8 | 2/2 | M | CP-11 | independent | none |
| CP-13 | implementation | 5/8 | 2/8 | 2/2 | M | CP-12 | independent | none |
| CP-14 | implementation | 4/8 | 2/8 | 2/2 | M | CP-13 | independent | none |
| CP-15 | implementation | 3/8 | 2/8 | 2/2 | M | CP-14 | independent | none |
| CP-16 | implementation | 4/8 | 2/8 | 2/2 | M | CP-15 | independent | none |
| CP-17 | implementation | 4/8 | 2/8 | 2/2 | M | CP-16 | independent | none |
| CP-18 | implementation | 3/8 | 2/8 | 2/2 | M | CP-17 | independent | none |
| CP-19 | audit | 4/20 | 2/20 | 0/0 | S | CP-18 | no-release | none |
| CP-20 | release | 5/12 | 2/12 | 0/0 | M | CP-19 | independent | none |

### Task CP-00: Inventory the implemented public CLI

**Task type:** `audit`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Read the compiled CLI definition and handlers; expand the ledger down to every parameter. Add bounded cards for any eligible domain not owned below. The audit handoff unlocks CP-01, but CP-00 becomes done only after inherited CP-06 release coverage.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-00; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every public command-tree node has a ledger row.
- [ ] Every argument has an evidence-backed disposition.
- [ ] Every eligible dynamic row has a bounded task owner.
- [ ] All source and documentation paths are enumerated for each owner.
- [ ] The implementation branch and external reservations are recorded.

**Verification:**

- [ ] Review the assigned ledger rows against the compiled command tree and consuming handlers; record a zero-unassigned result.
- [ ] Validate every task/source/documentation reference and dependency edge; attach the audit handoff record.

**Full-suite trigger:** `none: documentation audit; inherits CP-06`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** none; none for repository-local audit. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Reviewed ledger and audit handoff.

**Implementation write set:** N/A (audit only); plan-20260923-coverage.tsv and this plan/status/index. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** N/A.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Plan, ledger, status and date index only; no user-facing behavior change.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `S`.

**Version increment:** `none; inherited acceptance`.

**Release boundary:** `no-release`.

**C/D coverage from:** `CP-06`.

**Granularity:** `type=audit; axis=Inventory the implemented public CLI; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/20; VER=2/20; landing=0; prod-files=0; scope=S; deps=none; writeset=audit handoff; release=no-release; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-01: Complete static grammar coverage

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Retain AOT generation from the compiled clap tree. Introduce a minimal metadata attachment boundary with a working static generator consumer and a command-tree coverage guard; do not create a duplicate parser.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-01; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every public command path is reachable through generated static completion.
- [ ] Flag coverage matches the compiled tree.
- [ ] Alias visibility matches the declared CLI contract.
- [ ] Feature-gated command visibility matches the selected build.
- [ ] Unknown registration keys fail the coverage guard.
- [ ] Accepted CLI input behavior is unchanged.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_static_grammar --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-00 accepted audit handoff (not Lifecycle=done; CP-00 inherits CP-06); DEP-CP-01/04/07; DEP-CP-02. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/mod.rs; src/internal/completion/mod.rs; src/internal/completion/static_metadata.rs; src/command/completions.rs; src/cli.rs; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete static grammar coverage; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=6/8; VER=2/8; landing=2; prod-files=5; scope=M; deps=CP-00 accepted audit handoff (not Lifecycle=done; CP-00 inherits CP-06); writeset=serialized after CP-00; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-02: Complete static fixed-value suggestions

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Attach only choices already supported by the consuming command. Include Libra-only filters and agent names where the supported vocabulary is compile-time fixed. Do not convert an open string into a restrictive enum merely to obtain suggestions.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-02; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every fixed-values ledger row has the expected suggestions.
- [ ] Open-ended parameters retain their accepted inputs.
- [ ] Secrets have no enumerated suggestions.
- [ ] Creation names are not populated as existing-object selectors.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_static_values --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-01; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/static_metadata.rs; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete static fixed-value suggestions; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=CP-01; writeset=serialized after CP-01; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-03: Complete static filesystem hints

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Classify actual file/directory arguments across all implemented commands, including extension commands. Static filesystem hints do not claim Git index-aware filtering or repository object discovery.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-03; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every file-classified row has a file completion hint.
- [ ] Every directory-classified row has a directory completion hint.
- [ ] Revision/free-text arguments do not acquire path-only semantics.
- [ ] Quoted paths survive shell insertion.
- [ ] Arguments after -- follow the consuming command grammar.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_static_paths --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-02; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/static_metadata.rs; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete static filesystem hints; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=CP-02; writeset=serialized after CP-02; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-04: Support static completion for binary aliases

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Add --bin-name with default libra to AOT generation; accept only a bounded shell-safe name (ASCII letters/digits/_/-, length 1..64). Test actual lba invocation, not only output text.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-04; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Custom names register the requested executable.
- [ ] Invalid binary names produce usage exit 129.
- [ ] Default invocation preserves the static mode.
- [ ] JSON data preserves existing shell/script fields.
- [ ] The lba invocation completes successfully in each supported shell.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_static_alias --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `none: bounded command option, no shared/root surface change`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-03; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/command/completions.rs; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Support static completion for binary aliases; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=1; prod-files=1; scope=M; deps=CP-03; writeset=serialized after CP-03; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-05: Document and print static installation guidance

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Print per-shell AOT installation instructions from both installers. Never install shell files automatically. Include user-invoked file installation and lba instructions; keep Windows syntax separate.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-05; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Successful installs print valid static installation instructions.
- [ ] Alias instructions use the delivered --bin-name option.
- [ ] Installer guidance writes no completion/startup files.
- [ ] Existing installer behavior is preserved.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_install_guidance --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] `source .env.test && cargo nextest run --test compat_install_alias --no-tests=fail` — extend isolated installer tests for printed guidance and no startup-file writes.

**Full-suite trigger:** `T-1: installers`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-04; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** install.sh; install.ps1; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger; tests/compat/install_alias_test.rs. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard bundle; docs/installation.md; authorized backend installation page.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Document and print static installation guidance; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-04; writeset=serialized after CP-04; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-06: Accept and release the complete static stage

**Task type:** `release`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Reconcile the entire static ledger, collect real execution evidence for all five shells and both executable names, and publish the static-stage acceptance record. No dynamic code may start before this card completes.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-06; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every static ledger row has passing evidence or a justified non-applicable classification.
- [ ] Five-shell static execution evidence is complete.
- [ ] Feature/platform coverage has no unverified supported profile.
- [ ] CP-00 audit receives C/D coverage for its final artifact.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_static_stage --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-2: stage release and aggregation`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-05; CP-00 accepted audit handoff (its release coverage is acquired by this card); DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** N/A: release evidence and accompanying documentation only; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard bundle; this plan and coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=release; axis=Accept and release the complete static stage; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/12; VER=2/12; landing=0; prod-files=0; scope=M; deps=CP-05; writeset=serialized after CP-05; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-07: Introduce opt-in dynamic completion with a bounded read context

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** After CP-06 only, verify the pinned clap_complete dynamic API. Add --dynamic while retaining static default. Build read-only completion context and a registry with one working local-branch consumer (switch). Detect repository context using existing supported CLI location options; bypass normal write preflight. Do not call mutation-capable list handlers.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-07; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Dynamic mode is explicitly selected.
- [ ] Static output remains behaviorally compatible.
- [ ] The switch branch consumer returns matching local branches.
- [ ] Completion requests have zero persistent writes.
- [ ] Normal non-completion invocations retain their behavior.
- [ ] Unavailable data produces a clean empty response.
- [ ] The process-level deadline is enforced.
- [ ] Unsupported schema versions fail closed to empty candidates.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_context --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-06; DEP-CP-01/04/07; DEP-CP-02; DEP-CP-03. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** Cargo.toml feature only; src/main.rs; src/cli.rs; src/command/completions.rs; src/internal/completion/{mod.rs,registry.rs,context.rs,providers/refs.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Introduce opt-in dynamic completion with a bounded read context; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=8/8; VER=2/8; landing=4; prod-files=8; scope=M; deps=CP-06; writeset=serialized after CP-06; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-08: Complete implemented reference-valued arguments

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Extend the reference provider to every current revision/ref consumer, including Libra-only revision/metadata/deps options. Differentiate local branches, tags, remote-tracking refs and accepted special refs based on handler evidence; preserve expression syntax only where supported.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-08; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned reference row returns only its accepted ref classes.
- [ ] Ambiguous short refs use a valid disambiguating form.
- [ ] Literal prefix matching handles SQL wildcard characters correctly.
- [ ] Results are deterministically truncated after filtering.
- [ ] Creation destinations do not reuse existing-ref suggestions incorrectly.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_refs --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-07; DEP-CP-01/04/07; DEP-CP-05. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/refs.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete implemented reference-valued arguments; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-07; writeset=serialized after CP-07; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-09: Complete configured remotes and supported refspec contexts

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Reuse current read-only configuration resolution for remote names, including agent push --remote. Add refspec component suggestions only for existing accepted syntax using local information. Do not introduce network discovery or broaden refspec parsing.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-09; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned remote selector returns configured names.
- [ ] Supported refspec positions receive context-valid local candidates.
- [ ] No request contacts a remote endpoint.
- [ ] Legacy-only global configuration remains byte-for-byte unchanged.
- [ ] Credential-bearing URLs are not emitted as suggestions.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_remotes --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-08; DEP-CP-01/04/07; DEP-CP-03; DEP-CP-05. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/remotes.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete configured remotes and supported refspec contexts; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-08; writeset=serialized after CP-08; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-10: Complete registered worktree selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover existing paths for remove/lock/unlock/move-source and accepted add-target refs. Keep move destinations/new paths as creation/filesystem arguments. Include platform-specific existing worktree selectors where implemented.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-10; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned existing-worktree selector returns command-valid paths.
- [ ] New destination arguments retain creation semantics.
- [ ] Linked-worktree discovery uses the correct repository scope.
- [ ] Paths with whitespace survive shell insertion.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_worktrees --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-09; DEP-CP-01/04/07; DEP-CP-05. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/worktrees.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete registered worktree selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-09; writeset=serialized after CP-09; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-11: Complete stash selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Register actual stash selectors from the compiled tree. Reuse current stash-reference syntax and enumerate local entries without opening mutation paths.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-11; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned stash selector returns accepted stash references.
- [ ] Empty stash storage yields no candidates.
- [ ] Descriptions contain no file contents or sensitive payloads.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_stash --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-10; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/stash.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete stash selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=3/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-10; writeset=serialized after CP-10; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-12: Complete contextual agent-name selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover enable/disable/add/remove, session filters and other implemented agent-name inputs. Distinguish supported agents from enabled/observed agents according to each handler. Compile-time supported names remain available through static metadata.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-12; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Each agent-name selector uses its handler-defined name set.
- [ ] Public alias commands receive equivalent suggestions.
- [ ] No external agent binary or hook is executed.
- [ ] Static supported-agent suggestions remain available.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_agents --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-11; DEP-CP-01/04/07; DEP-CP-03. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/agents.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete contextual agent-name selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-11; writeset=serialized after CP-11; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-13: Complete session selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover session show/stop/resume/derive-tool-calls, graph, checkpoint list --session and any inventory-assigned session selector. Do not register nonexistent session export/delete commands.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-13; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned session selector has candidates.
- [ ] Identity scope matches the consuming handler.
- [ ] Lifecycle-sensitive selectors follow current eligibility rules.
- [ ] Descriptions exclude transcript/prompt payloads.
- [ ] Inserted IDs remain full valid identifiers.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_sessions --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-12; DEP-CP-01/04/07; DEP-CP-06. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/sessions.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete session selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-12; writeset=serialized after CP-12; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-14: Complete checkpoint selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover checkpoint show/export/rewind, using actual IDs and current visibility/eligibility rules. Do not introduce restore as a new command.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-14; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every assigned checkpoint selector has candidates.
- [ ] Identity scope matches the consuming handler.
- [ ] Candidate construction never materializes or exports a checkpoint.
- [ ] Descriptions exclude transcript/prompt payloads.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_checkpoints --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-13; DEP-CP-01/04/07; DEP-CP-06. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/checkpoints.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete checkpoint selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-13; writeset=serialized after CP-13; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-15: Complete workspace selectors

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover agent workspace show and worktree doctor selectors. Doctor may intentionally expose a broader diagnostic scope than ordinary workspace listings; preserve that documented difference.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-15; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Ordinary selectors follow normal workspace visibility.
- [ ] Diagnostic selectors follow the existing doctor scope.
- [ ] No request adopts or repairs workspace records.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_workspaces --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-14; DEP-CP-01/04/07; DEP-CP-06. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/workspaces.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete workspace selectors; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=3/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-14; writeset=serialized after CP-14; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-16: Complete automation rule names

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Read rule names from the current scoped automation configuration and attach to automation run --rule. History has no rule selector. Reuse parsing without evaluation or execution.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-16; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] run --rule returns configured rule names.
- [ ] Linked-worktree configuration resolution matches automation.
- [ ] Malformed configuration yields an empty response.
- [ ] No rule action or scheduling side effect is executed.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_automation --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-15; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/automation.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete automation rule names; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-15; writeset=serialized after CP-15; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-17: Complete operation references

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover op show/restore using the implemented operation-ID/index syntax and current visibility rules. Avoid serializing operation payloads merely to form candidates.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-17; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Operation selectors receive accepted references.
- [ ] Ordering agrees with operation index semantics.
- [ ] Descriptions exclude stored sensitive metadata.
- [ ] No snapshot restore path is invoked.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_operations --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-16; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/operations.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete operation references; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-16; writeset=serialized after CP-16; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-18: Complete existing layer names

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Cover layer enable/disable/remove/unapply --layer with scoped existing names. layer add name stays a creation argument; --source retains its path hint.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-18; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Existing-layer selectors receive scoped names.
- [ ] Creation arguments retain their static classification.
- [ ] No layer materialization or cleanup is invoked.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_dynamic_layers --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-1: shared completion metadata/provider contract`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-17; DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** src/internal/completion/{registry.rs,providers/layers.rs}; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard completion docs bundle and consuming-command pages assigned by the coverage ledger.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=implementation; axis=Complete existing layer names; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=3/8; VER=2/8; landing=2; prod-files=2; scope=M; deps=CP-17; writeset=serialized after CP-17; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-19: Reconcile all remaining implemented capability coverage

**Task type:** `audit`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Audit the ledger against the current compiled trees. Local config keys, index-aware paths, other named objects and feature-gated domains cannot disappear into this card: CP-00 must already have assigned bounded implementation cards, or this audit blocks and requires a reviewed plan amendment. No new runtime code is permitted here.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-19; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every eligible dynamic row has accepted implementation evidence.
- [ ] Every exclusion has a current handler-based reason.
- [ ] No removed or nonexistent command remains registered.
- [ ] All feature/platform profiles are reconciled.

**Verification:**

- [ ] Review the assigned ledger rows against the compiled command tree and consuming handlers; record a zero-unassigned result.
- [ ] Validate every task/source/documentation reference and dependency edge; attach the audit handoff record.

**Full-suite trigger:** `none: audit; inherits CP-20`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-18; none for repository-local audit. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Reviewed ledger and audit handoff.

**Implementation write set:** N/A (audit only); coverage ledger and plan/status/index. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** N/A.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Plan, ledger, status and date index only.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `S`.

**Version increment:** `none; inherited acceptance`.

**Release boundary:** `no-release`.

**C/D coverage from:** `CP-20`.

**Granularity:** `type=audit; axis=Reconcile all remaining implemented capability coverage; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=4/20; VER=2/20; landing=0; prod-files=0; scope=S; deps=CP-18; writeset=serialized after CP-18; release=no-release; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

### Task CP-20: Accept and release static plus dynamic coverage

**Task type:** `release`

**Lifecycle / Acceptance:** `pending` / empty

**Description:** Collect complete shell, scope, failure, no-write and process-budget evidence for the final tree. This release does not change the static default. Reconcile all inherited audit coverage and outstanding dependency records.

**Out of scope:** Other cards' object domains; unsupported underlying command behavior; network candidates; changes to the static-first barrier.

**Current evidence:** Fact baseline and coverage-ledger rows assigned to CP-20; refresh source anchors under ER-02. New ledger and test artifacts are planned, not already present.

**Acceptance criteria:**

- [ ] Every eligible ledger row is accepted.
- [ ] All five shells execute both modes successfully.
- [ ] The final full-suite run is green.
- [ ] CP-19 receives final-tree C/D coverage.
- [ ] No unresolved acceptance blocker remains.

**Verification:**

- [ ] `source .env.test && cargo nextest run --test command_test completions_final_acceptance --no-tests=fail` — planned test family (new), covering this card's acceptance predicates.
- [ ] Execute the assigned five-shell behavioral fixtures from the test matrix below; record exact shell versions, commands and results.

**Full-suite trigger:** `T-2: final release and aggregation`. Re-evaluate if actual files or behavior expand; no automatic exemption.

**Dependencies:** CP-19 accepted audit handoff (not Lifecycle=done; CP-19 inherits CP-20); DEP-CP-01/04/07. Serial predecessor edges include shared tests/docs/registry intersections.

**Deliverables:** Implemented behavior, regression evidence and synchronized documentation.

**Implementation write set:** N/A: release evidence and accompanying documentation only; tests/command/completions_test.rs; assigned documentation bundle; coverage ledger. All cards also update this plan, plan-status.md and the plan-long date-index row. Add no unlisted production file without revising the card first.

**Release write set:** Inherited: authoritative version surfaces + toolchain-refreshed Cargo.lock + artifacts.

**Files likely touched:** The declared write set; no broader fallback.

**Docs and compatibility impact:** Standard bundle; installation docs; plan/ledger/status/date index.

**Rollback mode:** `revert`; dependency order and published forward-release policy apply.

**Migration and rollback:** N/A: no schema writers. Dynamic readers obey ADR-CP-04 database-role allowlists.

**Security and privacy:** Inherit global constraints; isolates HOME/XDG/config databases and temporary repositories for runtime verification.

**Performance budget:** Inherit stage-specific budgets; audit/release records do not add runtime work.

**Estimated scope:** `M`.

**Version increment:** `patch; refresh actual version at execution`.

**Release boundary:** `independent`.

**C/D coverage from:** `self`.

**Granularity:** `type=release; axis=Accept and release static plus dynamic coverage; recovery=reverse-dependency revert with published forward release; complete=yes; self-contained=yes; AC=5/12; VER=2/12; landing=0; prod-files=0; scope=M; deps=CP-19 accepted audit handoff (not Lifecycle=done; CP-19 inherits CP-20); writeset=serialized after CP-19; release=independent; split-from=2026-09-23 dynamic-first draft; exception=N/A`.

## Evidence record format

Each card records tested HEAD/tree identity, source-anchor refresh date, actual command lines, feature profile, shell/OS/tool versions, exit status and sanitized result summary. Planned test names are not execution evidence. Store per-parameter test mappings in the coverage ledger and link release/review evidence from the card; do not record secrets or transcript contents. Audit handoff acceptance is an explicit review milestone, not Lifecycle=done: CP-01 may consume CP-00's reviewed artifact while CP-00 awaits CP-06 C/D coverage; CP-20 similarly consumes CP-19's reviewed artifact. This avoids a cyclic done-state prerequisite.

## Test matrix

| Area | Required behavioral evidence | Owner / execution |
|---|---|---|
| Inventory | No missing public node/arg, stale registration or unowned eligible domain; feature/platform profiles | CP-00/01/19; command-tree guard and ledger review |
| Static shells | bash/zsh/fish/powershell/elvish: load generated scripts in isolated profiles; invoke completion for nested commands, flags, aliases, fixed values and paths | CP-01..06; command_test::completions_static_* plus shell fixtures |
| Compatibility | Accepted input set, shell/script JSON data, 129 usage errors and default static mode | CP-01/02/04/07 |
| Dynamic shells | Install opt-in scripts; invoke real callbacks for applicable provider fixtures, including lba; test quotes, spaces, Unicode, --option=value, -- and multi-value arguments | CP-07..20; command_test::completions_dynamic_* |
| Context | Existing vs new names; full IDs vs display descriptions; same-name refs; repo/subdirectory/linked-worktree scope and accepted global location selectors | Each provider's assigned ledger rows |
| Fault isolation | Outside repo, missing/corrupt/locked/new-schema DB, malformed config, empty data, concurrent deletion, legacy-only config; clean empty response | CP-07 and provider-specific fixtures |
| No writes | Before/after files and DB state for isolated repo/global/system config, HOME/XDG; no migration receipts/locks/bootstrap artifacts, no hooks/network/vault calls | CP-07 and every applicable source |
| Performance | At least 10,000 candidate objects; matching prefix beyond first 500 unfiltered rows; stable truncation; cold process timing; lock/deadline expiry | CP-07/20; end-to-end subprocess fixtures, not provider-only timing |
| Installation | Per-shell syntax, static instructions, opt-in dynamic instructions added at CP-07; precreated rc files unchanged and absent rc files remain absent | CP-05/07; compat_install_alias plus shell runners |
| Whole-tree gates | fmt, clippy and required full nextest command | Triggered cards, CP-06 and CP-20 |

Use installed isolated shell runners or authorized CI environments. Missing runners block five-shell acceptance; generated-text checks alone do not substitute for execution. Shell-specific invocation fixtures are committed with their owning implementation card, with exact reproducible commands in its evidence. `--no-tests=fail` prevents nonexistent planned filters from producing false green. Shared test-helper changes trigger T-4; adding/modifying CI triggers T-1 and requires declared writes before editing.

## Traceability

| Source | Cards | Completion evidence |
|---|---|---|
| GAP-CP-01; actual CLI tree | CP-00..03, CP-19 | Coverage ledger and command-tree guards |
| GAP-CP-02; existing static generator | CP-04..06 | Five-shell static/alias/install evidence |
| GAP-CP-03; existing local refs/config/workflows | CP-07..11 | Read-only dynamic protocol and workflow fixtures |
| GAP-CP-03/04; actual Libra-only objects | CP-12..18 | Agent, automation, operation and layer fixtures |
| GAP-CP-05; existing scope/config contracts | CP-07 and every provider | No-write and identity isolation tests |
| Full implemented-surface coverage | CP-19/20 | No eligible unassigned/deferred local domain; final release evidence |

## Milestone acceptance and recovery

| Milestone | Completion | Recovery |
|---|---|---|
| M0 | CP-00 audit handoff, expanded card review and reservations recorded | Revert documentation amendment only |
| M1 static | CP-06 done/complete; CP-00 inherited coverage complete | Reverse affected static commits; publish a corrective version if already released |
| M2 dynamic | CP-20 done/complete; CP-19 inherited coverage complete | Users retain static mode; revert consumers before providers/engine; publish correction |

A consumer cannot remain enabled when its prerequisite provider is removed. A failed release never justifies dropping tests, rewriting published refs, deleting releases or marking remote-pending complete. Fix exposed bugs forward, rerun appropriate gates and record baseline failures under the template policy. Static completion remains available if dynamic mode must be disabled.

## Risk register

| Risk | Mitigation | Owner |
|---|---|---|
| Coverage inventory misses unique/feature-gated capabilities | Exhaustive compiled-tree ledger; no unassigned rows | CP-00/01/19 |
| Supplementary metadata changes parser validation | Before/after accepted-input tests; revise design instead of rejecting valid inputs | CP-02/03 |
| Dynamic API cannot preserve context or shell behavior | Pin verified dependency/API; revise design before implementation; never regress static mode | CP-07 |
| Configuration reads migrate or unlock | Explicit read-only context and legacy-only no-write fixtures | CP-07/09/12 |
| Identity leakage or unusable suggestions | Consumer-specific scope and state predicates | CP-10..18 |
| LIMIT mistaken for bounded work | End-to-end deadlines and large/locked-store tests | CP-07/20 |
| Shared files collide with existing plans | DEP-CP-02/04/05 reservations; serial local DAG | Every card |

## Performance and capacity summary

Static generation has no repository lookup. Dynamic responses return at most 500 candidates total; prefix filtering is literal, deterministic and applied before truncation. SQL parameters are bound, LIKE metacharacters escaped where applicable, and lack of an index never excuses an unbounded scan. Read deadlines include connection and lock waits. No persistent cache is introduced. No new schema index is silently added; inability to meet the budget requires a separate approved change or plan revision.

## Compatibility and documentation closure

- [ ] Standard completion bundle describes full static coverage and optional dynamic mode accurately.
- [ ] Consuming command pages in both languages and authorized backend pages match ledger-owned behavior.
- [ ] Installation pages cover the five actual shells with native syntax; user configuration is not edited automatically.
- [ ] Tests/INDEX.md and any compat registration/docs changes match actual new targets, or N/A is evidenced.
- [ ] docs/error-codes.md is updated only if new stable errors are introduced; none are planned.
- [ ] plan-status and plan-long date index match task acceptance evidence.

## Review log

No review result is asserted. Future verdict rows must use PASS or FAIL only. Preserve the prior draft's dual-review requirement for implementation approval; no external reviewer is invoked by this documentation rewrite.

| Round | Scope | Reviewer | Result | Findings disposition | Evidence |
|---|---|---|---|---|---|

## Non-goals and deferred items

| ID | Item | Reason | Reopen condition | Owner |
|---|---|---|---|---|
| DEFER-CP-01 | Additional shells such as Nushell | Preserve current five-shell support first; static-first does not imply rejecting static support for future shells | Explicit shell-support scope decision | Separate shell-support card |
| DEFER-CP-02 | Network-backed suggestions | Completion must be local and bounded | Explicit product/security decision | New plan |
| DEFER-CP-03 | Underlying capabilities not implemented by Libra | Outside completion responsibility, regardless of Git support | Owning feature lands; it must add completion coverage | Owning feature plan |

These deferrals cannot hide existing local capabilities. Free text, secrets, new identifiers and hidden internals are documented non-applicable classifications, not unfinished runtime providers.

## Completion criteria

- [ ] All card granularity summaries match the final scope; no unapproved EX waiver.
- [ ] Every non-deferred card is done/complete, including inherited audit coverage.
- [ ] M1 evidence predates the start of dynamic implementation.
- [ ] Every implemented public capability has a reviewed coverage disposition; every eligible local dynamic domain is delivered.
- [ ] Both modes retain five-shell behavioral evidence for the final tree.
- [ ] Final fmt and clippy gates pass.
- [ ] Final `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2` passes; exposed bugs are forward-fixed and rerun under the template policy.
- [ ] Required reviews have final PASS with blockers closed.
- [ ] Every independent release has signed/DCO, version parity, toolchain lockfile and applicable C/D evidence; no remote-pending remainder.
- [ ] Documentation, tracking, dependency records and recovery evidence are reconciled.

## Execution checklist

- [x] Rewrite the plan in English around the user's capability-led, static-first requirements and register it in tracking.
- [ ] Complete CP-00 audit handoff and any necessary scope-driven card additions.
- [ ] Obtain required implementation-plan review verdicts.
- [ ] Deliver CP-01..06 and accept M1.
- [ ] Deliver CP-07..18 and any inventory-added domain cards.
- [ ] Complete CP-19 audit handoff and CP-20 final acceptance.

## Final verification wave

F1: complete coverage ledger. F2: static and dynamic shell execution evidence. F3: fmt/clippy/full nextest green. F4: release, documentation and task-state reconciliation. All remain pending until implementation execution.
